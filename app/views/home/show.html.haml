#main-content.span8
  #content.row
    = render partial: 'carousel'

    .seguimiento.phase-header
      %h2
        %span
          4
        Sígueles...

    = render partial: 'sigueles'

    .span8
      %span Secci&oacute;n
      %input{type: :text}
      %inpuy{type: :button, class: [:btn, 'btn-primary']} Buscar
    
    #map

= render partial: 'common/right_column'

:javascript
  window.mapas.initialize({state: '#{@site.label.downcase.gsub(' ', '_').gsub('ó','o')}'});