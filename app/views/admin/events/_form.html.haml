.control-group
  .control-label Titulo
  .controls
    = form.text_field :title

.control-group
  .control-label Lugar
  .controls
    = form.text_field :venue

.control-group
  .control-label Fecha
  .controls
    = form.text_field :date

.control-group
  .control-label Categorias
  .controls
    = form.text_field :event_category_list

.control-group
  .control-label Descripci&oacute;n
  .controls
    = form.text_area :description, 'data-rich-text' => "true"

.form-actions
  = form.submit (@event.new_record? ? 'Crear' : 'Editar'), class: ['btn', 'btn-primary']