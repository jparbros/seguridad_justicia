%h1
  Editar Seguimiento

= form_for(@tracing, url: admin_representative_tracing_path(@representative, @tracing), class: 'form_element', method: :put) do |form|
  = render partial: 'form', object: form